{
  "name": "cf-ddns-client",
  "version": "1.0.0",
  "description": "Cloudflare API Consumer for custom DDNS Update Script.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "start-dev": "nodemon -r dotenv/config index.js",
    "start-service": "forever start --minUptime 1000 --spinSleepTime 1000 index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.20.0"
  },
  "devDependencies": {
    "dotenv": "^8.2.0",
    "nodemon": "^2.0.4"
  }
}